# DoS Detection Script

This script retrieves HTTP telemetry logs, aggregates requests per source IP, applies threshold-based detection, and triggers automated containment via enforcement endpoint.

Purpose: detection engineering training lab.
